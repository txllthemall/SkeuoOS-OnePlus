plugins { id 'com.android.application' }

android {
    namespace 'io.github.txllthemall.skeuoos'
    compileSdk 36

    defaultConfig {
        applicationId 'io.github.txllthemall.skeuoos'
        minSdk 26
        targetSdk 36
        versionCode 5
        versionName '4.1.0'
    }

    flavorDimensions += 'appearance'
    productFlavors {
        color {
            dimension 'appearance'
            // Keep the original package id so the color build upgrades the existing pack.
        }
        clear {
            dimension 'appearance'
            // Separate package id so Color and Clear can be installed side by side.
            applicationIdSuffix '.clear'
            versionNameSuffix '-clear'
        }
    }

    buildTypes {
        release { minifyEnabled false }
    }
}

// One geometry source feeds both appearance flavors. Always use the release
// vector driver so local/Gradle generation cannot silently fall back to an old
// experimental v4.1 renderer.
tasks.register('generateLiquid27Icons', Exec) {
    workingDir rootProject.projectDir
    commandLine 'python3', 'tools/generate_liquid27_release.py', '--variant', 'all'
}
